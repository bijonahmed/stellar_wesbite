// app/(public)/login/layout.jsx
export const metadata = {
  title: "Register", // specific title for login page
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}
